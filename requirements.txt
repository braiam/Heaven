# web UI
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0

# fetch.py (grab your account data from the game server)
frida>=16.0
msgpack>=1.0
pycryptodome>=3.20
curl_cffi>=0.7

# TT capture (mitmproxy addon)
mitmproxy>=10.0
